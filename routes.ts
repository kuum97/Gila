export const publicRoutes = ['/'];

export const authRoutes = ['/sign-in', '/sign-up'];

export const apiAuthPrefix = 'api/auth';
export const apiUploadThingPrefix = 'api/uploadthing';

export const DEFAULT_LOGIN_REDIRECT = '/activity-list';
